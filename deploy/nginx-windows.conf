worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    server {
        listen 80;
        server_name yourdomain.com www.yourdomain.com;

        root C:/www/traydbook/dist;
        index index.html;

        # Serve the React SPA
        location / {
            try_files $uri $uri/ /index.html;
        }

        # Proxy API requests to Express server
        location /api/ {
            proxy_pass http://127.0.0.1:3001;
            proxy_http_version 1.1;
            # Use explicit backend host — do not forward client Host header
            proxy_set_header Host $server_name;
            # Close upgrade headers to prevent H2C smuggling
            proxy_set_header Upgrade "";
            proxy_set_header Connection "close";
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Static asset caching
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # HTTPS — uncomment after placing your SSL certificate files
    # server {
    #     listen 443 ssl;
    #     server_name yourdomain.com www.yourdomain.com;
    #
    #     ssl_certificate     C:/ssl/yourdomain.com/fullchain.pem;
    #     ssl_certificate_key C:/ssl/yourdomain.com/privkey.pem;
    #     ssl_protocols       TLSv1.2 TLSv1.3;
    #     ssl_ciphers         HIGH:!aNULL:!MD5;
    #
    #     root C:/www/traydbook/dist;
    #     index index.html;
    #
    #     location / {
    #         try_files $uri $uri/ /index.html;
    #     }
    #
    #     location /api/ {
    #         proxy_pass http://127.0.0.1:3001;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $server_name;
    #         proxy_set_header Upgrade "";
    #         proxy_set_header Connection "close";
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #
    #     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    #         expires 1y;
    #         add_header Cache-Control "public, immutable";
    #     }
    # }
}
